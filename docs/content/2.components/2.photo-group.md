---
title: PhotoGroup
description: Groups multiple photos or albums under a single shared lightbox.
navigation:
  icon: i-lucide-images
---

# PhotoGroup

The `PhotoGroup` component provides a shared lightbox context for multiple [`Photo`](/components/photo) or [`PhotoAlbum`](/components/photo-album) components. It supports two modes: **auto-collection** (children register themselves) and **explicit** (you provide the photos array).

::collection-playground
::

## Usage
Use auto mode when the rendered children define the collection. Use explicit mode when your data source, custom layout, or external controls should own it directly.

## Props

::field-group
  ::field{name="photos" type="PhotoItem[] | any[]"}
  Explicit photos list. When provided, the group operates in **explicit** mode and ignores child registrations. When omitted, uses **auto-collection** mode.
  ::

  ::field{name="photoAdapter" type="PhotoAdapter"}
  Transforms each item in `photos` into a `PhotoItem`. Only used in explicit mode.
  ::

  ::field{name="lightbox" type="boolean | Component" defaultValue="true"}
  Lightbox to render. `true` = default lightbox, `false` = no lightbox, or pass a custom component.
  ::

  ::field{name="transition" type="LightboxTransitionOption"}
  Transition mode for lightbox open/close animations. See [Transitions guide](/guides/transitions).
  ::
::

## Slots

| Slot | Props | Description |
|---|---|---|
| `default` | `{ open, setThumbRef }` | Content area. In auto mode, place `Photo`/`PhotoAlbum` children here. In headless mode, use `open` and `setThumbRef` to build custom layouts. |
| `toolbar` | `{ activeIndex, activePhoto, count }` | Custom lightbox toolbar content. |
| `caption` | `{ photo, activeIndex }` | Custom lightbox caption content. |
| `slide` | `{ photo, index, width, height }` | Custom lightbox slide content. |

## Exposed Methods

Access these via a template ref:

::field-group
  ::field{name="open" type="(photoOrIndex?: PhotoItem | number) => Promise<void>"}
  Opens the lightbox. Pass a photo object, an index, or nothing (defaults to index 0).
  ::

  ::field{name="close" type="() => Promise<void>"}
  Closes the lightbox with the configured transition.
  ::
::

## Behavior

- **Auto mode** (`photos` prop omitted): Child `Photo` and `PhotoAlbum` components register via provide/inject. Photo order matches DOM insertion order.
- **Explicit mode** (`photos` prop set): The provided array is used directly. Child registrations are ignored.
- **Slot forwarding:** The `#toolbar`, `#caption`, and `#slide` slots are forwarded into the lightbox component via injection.
