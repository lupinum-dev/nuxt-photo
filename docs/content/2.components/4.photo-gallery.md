---
title: PhotoGallery
description: Deprecated convenience wrapper around PhotoGroup + PhotoAlbum.
navigation:
  icon: i-lucide-layout-template
---

# PhotoGallery

::callout{type="warning"}
`PhotoGallery` is **deprecated**. Use [`PhotoGroup`](/components/photo-group) + [`PhotoAlbum`](/components/photo-album) directly instead — it gives you more control and makes the component hierarchy explicit.
::

## What It Does

`PhotoGallery` is a thin convenience wrapper that combines `PhotoGroup` and `PhotoAlbum` into a single component. It exists for simple cases where you just want a grid with a lightbox and don't need the flexibility of composing the pieces yourself.

## Migration

Replace `PhotoGallery` with the explicit composition:

::code-group
```vue [Before (deprecated)]
<PhotoGallery :photos="photos" layout="masonry" :spacing="8" />
```

```vue [After (recommended)]
<PhotoGroup>
  <PhotoAlbum :photos="photos" layout="masonry" :spacing="8" />
</PhotoGroup>
```
::

Or even simpler — `PhotoAlbum` already includes its own lightbox when not inside a `PhotoGroup`:

```vue
<PhotoAlbum :photos="photos" layout="masonry" :spacing="8" />
```

## Props

`PhotoGallery` accepts the same props as [`PhotoAlbum`](/components/photo-album) — they're passed through directly.

::field-group
  ::field{name="photos" type="PhotoItem[] | any[]" required}
  Array of photos.
  ::

  ::field{name="layout" type='AlbumLayout | "rows" | "columns" | "masonry" | "bento"' defaultValue='"rows"'}
  Layout algorithm.
  ::

  ::field{name="spacing" type="ResponsiveParameter<number>" defaultValue="8"}
  Gap between images.
  ::

  ::field{name="padding" type="ResponsiveParameter<number>" defaultValue="0"}
  Outer padding around images.
  ::

  ::field{name="adapter" type="ImageAdapter"}
  Custom image adapter.
  ::

  ::field{name="lightbox" type="boolean | Component" defaultValue="true"}
  Whether to enable the lightbox.
  ::
::
