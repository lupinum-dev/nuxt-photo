---
title: Stability Contract
description: What is stable, what is advanced, and what is internal.
---

Nuxt Photo is still pre-1.0, but the public contract is intentionally split:

- Root imports from `@nuxt-photo/core`, `@nuxt-photo/vue`, `@nuxt-photo/recipes`, and `@nuxt-photo/nuxt` are the stable surface.
- `@nuxt-photo/engine` is public and intentionally low-level. Use it when you need the framework-free lightbox orchestration layer directly.
- Anything undocumented is internal, even if it happens to be exported today.

Practical rule:

- Use recipe components for the happy path.
- Use `@nuxt-photo/vue` primitives and composables when you need custom UI.
- Use `@nuxt-photo/engine` only when you are deliberately building below the Vue layer.

If you build on the engine layer, expect more churn across breaking releases than you would from the root Vue entries.

## See also

:::read-more{to="/docs/reference/extension-api" title="Engine and customization"}
:::

:::read-more{to="/docs/components/lightbox-primitives" title="Lightbox primitives"}
:::
