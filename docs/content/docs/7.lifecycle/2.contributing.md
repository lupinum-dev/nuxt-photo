---
title: Contributing
description: Set up the repo, run the right checks, and update docs with code changes.
---

Use the contributor guide when you are working in the repo instead of consuming the published package.

Nuxt Photo keeps the full contributor workflow in the root [`CONTRIBUTING.md`](https://github.com/lupinum-dev/nuxt-photo/blob/main/CONTRIBUTING.md). That file covers:

- workspace layout
- local development commands
- required checks before review
- when code changes require doc updates
- where the active docs live in the repo

## Contributor workflow at a glance

Most changes follow the same path:

1. Work in the smallest package or app that proves the behavior.
2. Update public docs in the same change when behavior, API, defaults, or examples moved.
3. Run the narrowest checks while iterating.
4. Run the full relevant gate before review.

## Before you open a pull request

At minimum:

- work against the smallest package or app that proves the change
- update docs in the same change when public behavior moved
- run the relevant verification gate before review

## Verification gate

The normal pre-review checks are:

```bash
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm test:module-package
pnpm size
pnpm audit --audit-level moderate
pnpm release:pack
```

Use `pnpm build:docs` when you changed the docs app, docs content, or shared components that affect docs rendering.

Use `pnpm release:pack` before publishing. It packs the public packages with pnpm and verifies that tarball metadata is ready for npm.

## Test commands by area

| Area                  | Command                                                                                                                 |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Public exports        | `pnpm exec vitest run packages/vue/test/publicExports.test.ts packages/nuxt/test/publicExports.test.ts`                 |
| Photo validation      | `pnpm exec vitest run packages/vue/test/core/photo-normalize.test.ts packages/vue/test/recipes/recipeContracts.test.ts` |
| Recipes               | `pnpm exec vitest run packages/vue/test/recipes/recipeContracts.test.ts packages/vue/test/recipes/carousel.test.ts`     |
| Lightbox runtime      | `pnpm exec vitest run packages/vue/test/lightboxRuntime.test.ts`                                                        |
| Embla integration     | `pnpm exec vitest run packages/vue/test/recipes/emblaSnapState.test.ts packages/vue/test/recipes/carousel.test.ts`      |
| Nuxt module           | `pnpm exec vitest run packages/nuxt/test/module.test.ts packages/nuxt/test/runtime-plugin.test.ts`                      |
| Package/release truth | `pnpm exec vitest run packages/nuxt/test/packageConsistency.test.ts && pnpm release:pack`                               |

## Contributor risk map

Safe first changes:

- typo fixes and docs examples that do not change API
- CSS theme variables and non-structural visual tweaks
- narrow tests for already documented behavior

Medium-risk changes:

- recipe props or slots
- photo validation policy
- layout defaults
- Nuxt module options
- public export changes

Senior-review changes:

- lightbox lifecycle, open/close races, scroll locking, or focus management
- gesture, pan, zoom, and pointer handling
- Embla grouping, private snap-state access, or carousel navigation semantics
- package exports, release tooling, or publish workflow

## API change checklist

Before changing public API:

1. Identify the package entry point: `@nuxt-photo/vue`, `@nuxt-photo/nuxt/app`,
   component props, slots, module options, or app config.
2. Update exact export tests or component/module contract tests.
3. Update the public API docs and relevant component/config docs.
4. Decide whether the change is a hard cutover or requires an explicit migration
   note.
5. Run the smallest focused tests first, then the relevant full gate.

## Docs source of truth

Public docs live in `docs/content/docs/**`. Root docs like `README.md`, `CHANGELOG.md`, and `CONTRIBUTING.md` stay selective and support the GitHub front door, release history, and contributor workflow.

Because the docs app search indexes the `docs` collection, the public docs pages in this lifecycle section should carry the important contributor guidance themselves instead of acting as empty signposts.

## Next step

Open the root contributor guide before starting repo work so you use the right package, dev loop, and verification commands.
