---
title: Customization surfaces
description: Current customization surfaces in Nuxt Photo and when to use each one.
---

Use this page to look up the supported customization surfaces.

## Surfaces

| Surface                | Import from       | Use it for                                                           | Notes                                            |
| ---------------------- | ----------------- | -------------------------------------------------------------------- | ------------------------------------------------ |
| `useLightboxProvider`  | `@nuxt-photo/vue` | Building a custom lightbox UI with Vue primitives                    | Primary advanced Vue entrypoint                  |
| `resolveSlide` option  | `@nuxt-photo/vue` | Replacing slide rendering in a custom provider                       | Return `null` to keep the default image slide    |
| Lightbox primitives    | `@nuxt-photo/vue` | Custom overlays, viewports, slides, controls, captions, and triggers | Stay on this layer for custom Vue UI             |
| `LightboxComponentKey` | `@nuxt-photo/vue` | Replacing the default recipe lightbox globally                       | Provided through Vue injection                   |
| `ImageAdapterKey`      | `@nuxt-photo/vue` | Providing a default image adapter                                    | Use when module-level image wiring is not enough |
| `LightboxDefaultsKey`  | `@nuxt-photo/vue` | Setting shared default lightbox options                              | Vue-only customization hook                      |
| `PhotoGroup` `trigger` | `@nuxt-photo/vue` | Custom thumbnail layouts that keep the recipe lightbox               | Available in the custom-layout slot              |
| Recipe slots           | `@nuxt-photo/vue` | Thumbnails, built-in lightbox slots, and per-photo slide overrides   | Use before rebuilding the lightbox               |

## When to use which

- Build a custom lightbox UI with `useLightboxProvider` plus Vue primitives.
- Use `PhotoGroup`'s `trigger()` slot helper when only the thumbnail layout is custom.
- Use recipe slots when the default component is right but one rendered region needs custom markup.
- Override the default recipe lightbox globally with `LightboxComponentKey`.
- Provide a default image adapter with `ImageAdapterKey`.

## Examples

### Global lightbox override

```ts
import { LightboxComponentKey } from '@nuxt-photo/vue'
import MyLightbox from '~/components/MyLightbox.vue'

export default defineNuxtPlugin((nuxtApp) => {
  nuxtApp.vueApp.provide(LightboxComponentKey, MyLightbox)
})
```

## See also

:::read-more{to="/docs/guides/customize-built-in-lightbox" title="Customize the built-in lightbox"}
:::

:::read-more{to="/docs/guides/global-lightbox-override" title="Use a custom lightbox globally"}
:::

:::read-more{to="/docs/guides/custom-lightbox" title="Build a custom lightbox"}
:::

:::read-more{to="/docs/getting-started/stability" title="Stability contract"}
:::
